def is_prime?(n)
  (2..n**0.5).each { |i| return false if n%i == 0 }
  true
end

def populate_primes(max)
  primes = []
  (2..max).each { |i| primes.append(i) if is_prime?(i)}
  primes
end

p populate_primes(20)
